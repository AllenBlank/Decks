<h1>Listing cards</h1>
<%= will_paginate inner_window: 0, outer_window: 0, previous_label: '←', next_label: '→'  %>
<table>
  <tbody>
    <% @cards.each do |card| %>
      <tr>
        <td><%= link_to image_tag( card.low_res_url ), card %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
